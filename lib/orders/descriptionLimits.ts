/** Hard floor — empty / shorter than this cannot submit. */
export const MIN_PROJECT_DESCRIPTION_LENGTH = 30;

/** Soft quality cue — at or below this stays red; above turns green. */
export const PROJECT_DESCRIPTION_SOFT_GOOD = 60;
