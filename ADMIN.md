# مستندات جامع پنل مدیریت جار (Jaar Admin Documentation)

این مستند، راهنمای مرجع معماری، قابلیت‌ها، ساختار مدل‌ها، اکشن‌های سفارشی، مکانیزم‌های امنیتی و نحوه توسعه پنل مدیریت یکپارچه‌ی پلتفرم **جار (Jaar)** است. این پنل به طور کامل با کتابخانه‌ی `@premieroctet/next-admin` روی فریم‌ورک Next.js 14 App Router و دیتابیس SQLite/Prisma ORM بازسازی و مستقر گردیده است.

---

## ۱. معماری و ساختار فایل‌های پنل

پنل مدیریت جدید جایگزین کدهای پراکنده و منسوخ قبلی شده و با استفاده از معماری ماژولار زیر سازمان‌دهی شده است:

```text
├── app/
│   ├── admin/
│   │   └── [[...nextadmin]]/
│   │       └── page.tsx              # روت سرور اصلی ادمین، تزریق داشبورد و کنترل احراز هویت
│   ├── api/
│   │   └── admin/
│   │       └── [[...nextadmin]]/
│   │           └── route.ts          # هندلر API روت‌های NextAdmin با احراز هویت در تمام متدها
│   └── actions/
│       └── adminActionHandlers.ts    # سرور اکشن‌های کسب‌وکار (لغو سفارش، رد/تایید نمونه‌کار، لاگ حسابرسی)
├── components/
│   └── admin/
│       ├── AdminDashboard.tsx        # داشبورد تحلیلی و کارت‌های شاخص عملکرد (Tremor + Heroicons)
│       ├── CancelOrderDialog.tsx     # مودال اختصاصی لغو سفارش و ثبت یادداشت اداری
│       ├── RejectPortfolioDialog.tsx # مودال اختصاصی رد نمونه‌کار، ثبت دلیل و ارسال خودکار نوتیفیکیشن
│       └── OrderStatusSelect.tsx     # کامپوننت انتخاب وضعیت‌های هشت‌گانه سفارش با برچسب فارسی
├── lib/
│   ├── admin/
│   │   ├── options.tsx               # پیکربندی جامع NextAdmin (۲۱ مدل، سایدبار، ترجمه‌ها، اکشن‌ها)
│   │   └── formatters.tsx            # فرمترهای داده‌های JSON خام، چیپ‌های متنی و گالری تصاویر
│   └── auth/
│       ├── admin.ts                  # توابع احراز هویت و بررسی دسترسی سطح مدیر سیستم
│       └── session.ts                # رمزنگاری و مدیریت کوکی توکن‌های JWT نشست کاربران
└── ADMIN.md                          # همین فایل مستندات جامع
```

---

## ۲. جدول مدل‌های دیتابیس (۲۱ مدل Prisma)

تمامی ۲۱ مدل سیستم در سایدبار پنل در ۷ گروه منطقی دسته‌بندی شده‌اند:

| نام مدل در پریزما | عنوان فارسی در پنل | دسته‌بندی سایدبار | سطح دسترسی (Permissions) | توضیحات و فیلدهای سفارشی‌شده |
| :--- | :--- | :--- | :--- | :--- |
| **Order** | سفارش‌های پروژه‌ها (Orders) | سفارش‌ها و مارکت‌پلیس | کامل (Create/Edit/Delete) | مدیریت وضعیت ۸گانه، هوک beforeDb، مودبورد تصاویر، دیالوگ لغو، ثبت لاگ خودکار |
| **ProjectInterest** | پیشنهادات متخصصان (Interests) | سفارش‌ها و مارکت‌پلیس | کامل | پیشنهاد قیمت و ساعت متقاضیان پروژه‌ها با رابطه دوطرفه به Order و User |
| **User** | کاربران و اعضا (Users) | کاربران و متخصصان | کامل | نمایش فیلدهای تخصصی، نقش‌ها، فرمت چیپ تگ‌های مهارت، تجهیزات و نوع لوکیشن |
| **SpecialistProfile** | پروفایل‌های متخصصان (Specialists) | کاربران و متخصصان | کامل | فرمتر چیپ دسته‌بندی‌های انتخابی (`selectedCategories`) و مشخصات حساب متخصص |
| **PortfolioItem** | نمونه‌کارهای متخصصان (Portfolio) | کاربران و متخصصان | کامل | پیش‌نمایش تصویر کاور، اکشن تایید تکی/گروهی، دیالوگ رد با دلیل و ارسال پیام |
| **Expert** | عکاسان ویژه آزمایشی (Experts) | کاربران و متخصصان | کامل | جدول لیست متخصصان برگزیده با قابلیت جستجو بر اساس شهر، نام و تگ‌ها |
| **Transaction** | تراکنش‌های اشتراک (Transactions) | مالی و اشتراک‌ها | کامل | ثبت رکوردهای مالی خرید پلن، کد رهگیری، درگاه و شناسه فاکتور |
| **WithdrawalRequest** | درخواست‌های تسویه حساب (Withdrawals) | مالی و اشتراک‌ها | کامل | ثبت شماره شبا، مبلغ درخواستی، وضعیت بررسی، تاریخ و نام صاحب حساب |
| **Plan** | تعرفه‌ها و پلن‌های اشتراک (Plans) | مالی و اشتراک‌ها | کامل | نمایش چیپ ویژگی‌های پلن (`features`)، قیمت، دوره روزانه و فعال‌سازی |
| **DiscountCode** | کدهای تخفیف (Discount Codes) | مالی و اشتراک‌ها | کامل | تعیین درصد تخفیف، سقف مجاز، تاریخ انقضا و حداکثر استفاده |
| **GalleryProject** | پروژه‌های گالری تحویل فایل | گالری آنلاین و فروش عکس | کامل | آلبوم‌ها و پروژه‌های تحویل مستقیم عکس با کارفرما و وضعیت انتشار |
| **GalleryPhoto** | تصاویر گالری (Gallery Photos) | گالری آنلاین و فروش عکس | کامل | عکس‌های باکیفیت و واترمارک شده با فرمتر پیش‌نمایش تصویر و سایز فایل |
| **GalleryOrder** | سفارش‌های خرید عکس گالری | گالری آنلاین و فروش عکس | کامل | تراکنش‌های سفارش فایل گالری با مبالغ تفکیک‌شده و وضعیت پرداخت |
| **GalleryPurchase** | خریدهای نهایی‌شده گالری | گالری آنلاین و فروش عکس | کامل | لیست عکس‌های خریداری‌شده با فرمتر چیپ شناسه‌های عکس (`purchasedPhotoIds`) |
| **Course** | دوره‌های جارآموز (Courses) | آکادمی جارآموز | کامل | عنوان دوره، ویدیو معرفی، مدرس، طول مدت، قیمت دوره و اسلاگ یکتا |
| **Purchase** | خریدهای جارآموز (Course Purchases) | آکادمی جارآموز | کامل | فاکتورهای دوره‌های آموزشی، کاربر خریدار و وضعیت نهایی پرداخت |
| **Notification** | اعلان‌های کاربران (Notifications) | سیستم و ارتباطات | کامل | پیام‌های درون‌برنامه‌ای، پیامک، ایمیل، کانال و وضعیت خوانده شدن پیام |
| **NotificationTemplate** | قالب‌های پیام سیستم (Jarchi) | سیستم و ارتباطات | کامل | قالب‌های استاندارد نوتیفیکیشن با متغیرهای پویا (مانند رد نمونه‌کار) |
| **PwaSettings** | تنظیمات PWA و مانیفست | سیستم و ارتباطات | کامل | پیکربندی هویت PWA، آیکون‌ها، رنگ‌های پوسته و توضیحات اپلیکیشن |
| **Project** | لیدهای مشاوره فرم قدیمی (Leads) | سیستم و ارتباطات | کامل | لیدها و درخواست‌های قدیمی مشاوره و خدمات فرم با فرمتر جزئیات سرویس |
| **AuditLog** | لاگ‌های حسابرسی و تغییرات (Audit Logs) | امنیت و حسابرسی | **فقط خواندنی (`[]`)** | غیرفعال بودن دکمه ایجاد، حذف و ویرایش؛ ثبت شفاف عامل، عملیات و زمان |

---

## ۳. اکشن‌های سفارشی کسب‌وکار (Custom Business Actions)

### ۱. لغو سفارش (`cancel-order`)
- **رابط کاربری**: دیالوگ مودال (`CancelOrderDialog.tsx`) که دکمه آن تنها زمانی نمایش داده می‌شود که وضعیت سفارش `CANCELLED` یا `COMPLETED` نباشد (`canExecute`).
- **منطق سرور**: تابع `cancelOrderAction` در `app/actions/adminActionHandlers.ts`.
- **عملیات**:
  1. بررسی مجوز ادمین.
  2. اعتبارسنجی ثبت علت لغو.
  3. تغییر وضعیت سفارش به `CANCELLED` و ثبت علت در فیلد `adminCancelNote`.
  4. درج خودکار یک رکورد در جدول `AuditLog` با اکشن `ORDER_CANCELLED`.
  *(عملیات کاملاً مبتنی بر حسابداری داخلی است و هیچ ریکوئستی به درگاه‌های بیرونی ارسال نمی‌کند).*

### ۲. رد نمونه‌کار متخصص (`reject-portfolio`)
- **رابط کاربری**: دیالوگ مودال اختصاصی (`RejectPortfolioDialog.tsx`) در بخش جزئیات و لیست `PortfolioItem`.
- **منطق سرور**: تابع `rejectPortfolioAction` در `app/actions/adminActionHandlers.ts`.
- **عملیات**:
  1. بررسی مجوز ادمین.
  2. به‌روزرسانی فیلد `reviewStatus` نمونه‌کار به `REJECTED` و درج `rejectionReason`.
  3. ایجاد خودکار یا فراخوانی قالب نوتیفیکیشن `portfolio-rejected-jaramooz`.
  4. ارسال نوتیفیکیشن اختصاصی درون‌برنامه‌ای برای متخصص همراه با درج علت رد اثر و لینک تشویقی به آکادمی جارآموز (`/jaramooz`).
  5. ثبت خودکار رویداد در `AuditLog` با اکشن `PORTFOLIO_REJECTED`.

### ۳. تایید سریع نمونه‌کار (`approve-portfolio`)
- **رابط کاربری**: اکشن سروری مستقیم (`action`) در صفحه لیست و جزئیات `PortfolioItem` که به‌صورت تکی یا توده‌ای (Bulk) روی چند رکورد هم‌زمان قابل اجراست.
- **منطق سرور**: تابع `approvePortfolioAction`.
- **عملیات**: تبدیل وضعیت به `APPROVED`، خالی کردن دلیل رد قبلی و درج رکوردهای حسابرسی در `AuditLog`.

### ۴. مدیریت وضعیت سفارش (`Order.status`)
- فیلد `status` در فرم ویرایش سفارش به کامپوننت اختصاصی `OrderStatusSelect.tsx` متصل است که دقیقاً ۸ وضعیت استاندارد سیستم را با برچسب‌های شفاف فارسی ارائه می‌دهد:
  - `PENDING_DEPOSIT`: در انتظار پرداخت بیعانه
  - `DEPOSIT_PAID`: بیعانه پرداخت شد
  - `MATCHING`: در حال تطبیق و صف متخصصان
  - `HAS_APPLICANTS`: دارای متقاضی متخصص
  - `AWAITING_SPECIALIST_CONFIRMATION`: در انتظار تایید نهایی متخصص
  - `CONFIRMED`: قطعی و تایید شده
  - `COMPLETED`: انجام شده و تحویل نهایی
  - `CANCELLED`: لغو شده
- **اعتبارسنجی در لایه دیتابیس (beforeDb Hook)**: در `lib/admin/options.tsx` در صورت تلاش برای ارسال هر مقدار غیرمجاز، هوک استثنای `HookError` صادر می‌کند و تراکنش را ریجکت می‌نماید. همچنین هرگونه تغییر وضعیت در این فرم بلافاصله در `AuditLog` به صورت خودکار ثبت می‌شود.

### ۵. انتساب متخصص منتخب (`selectedSpecialist`)
- از طریق رابطه مستقیم `selectedSpecialistId` در فرم ویرایش سفارش انجام می‌شود.
- فیلد به صورت Typeahead جستجوپذیر کاربران فعال را فیلتر کرده و متخصص را به سفارش منتسب می‌سازد.

---

## ۴. مرکز فرماندهی و داشبورد شاخص‌های کلیدی (KPI Dashboard)

مسیر اصلی `/admin` بدون هیچ پیش‌فرضی به عنوان یک داشبورد زنده‌ی تحلیلی پیاده‌سازی شده است:

### ۱. کارت اصلی اضطراری (Hero Card)
- **سفارش‌های در انتظار اقدام**: سفارش‌هایی با وضعیت `MATCHING` یا `HAS_APPLICANTS` که بیش از ۲۴ ساعت در سیستم معطل مانده‌اند یا هنوز متخصص منتخب برای آن‌ها تعیین نشده است.
- دارای برچسب پالس اضطراری و دکمه دسترسی مستقیم.

### ۲. کارت‌های ثانویه عملکرد
- **سفارش‌های ثبت‌شده امروز**: شمارش لحظه‌ای تمام سفارش‌های ورودی از ابتدای بامداد امروز.
- **متخصصان در انتظار بررسی نمونه‌کار**: شمارش فایل‌های منتظر تایید ناظر کیفی.
- **درآمد ماه جاری جارآموز**: مجموع فروش موفق دوره‌های آکادمی در ۳۰ روز اخیر به تومان.

### ۳. نمودار تحلیلی ۳۰ روزه با Tremor
- رسم مساحت (Area Chart) روند ثبت سفارش‌ها در ۳۰ روز گذشته با استفاده از `@tremor/react`.
- **امنیت در نمایش RTL و موبایل**: برای جلوگیری از به هم ریختگی محاسبات SVG در صفحات RTL و بیرون‌زدگی در صفحات موبایل (۳۹۰ پیکسل)، نمودار در یک محفظه با اسکرول افقی نرم، اسکلت LTR و متن راست‌چین محصور شده است.

### ۴. جدول سفارش‌های نیازمند پیگیری فوری
- پیش‌نمایش آخرین ۵ سفارش در صف مانده همراه با نام کارفرما، شماره تماس، شاخه خدمات و لینک دسترسی آنی به فرم ویرایش.

---

## ۵. سیستم فرمتر داده‌های خام (Raw JSON Cleaners)

فیلدهایی که در دیتابیس به صورت آرایه یا رشته‌های JSON ذخیره می‌شوند، به جای متن ناخوانای خام با فرمترهای بصری نمایش داده می‌شوند:

1. **`RenderBadges`**: تبدیل آرایه‌های جیسون یا رشته‌های جدا شده با کاما به بچ‌چیپ‌های رنگی زیبا:
   - `User.specialistRoles`: چیپ‌های آبی نقش‌های تخصصی
   - `User.pricingGenres`: چیپ‌های سبز سبک‌های قیمت‌گذاری
   - `User.locationTypes`: چیپ‌های خاکستری لوکیشن‌های کاری
   - `User.equipment`: چیپ‌های تجهیزات و دوربین‌ها
   - `SpecialistProfile.selectedCategories`: دسته‌های منتخب عکاسی
   - `Plan.features`: امکانات پلن‌های اشتراک
   - `GalleryPurchase.purchasedPhotoIds`: شناسه‌های عکس‌های خریداری‌شده
2. **`RenderImageGallery`**: تبدیل آرایه‌های URL به تامبنیل‌های مربعی تمیز همراه با قابلیت زوم هاور و باز شدن تصویر باکیفیت اصلی در تب جدید (`Order.moodboardUrls`).
3. **ویرایشگر ساختاریافته**: در تمام این فیلدها در تب ویرایش، حالت `format: "json"` فعال شده است تا از وارد کردن مقادیر مخدوش جلوگیری شود.

---

## ۶. سیستم حسابرسی و لاگ تغییرات (Audit Log)

- **جدول دیتابیس**:
  ```prisma
  model AuditLog {
    id          String   @id @default(cuid())
    actorId     String
    action      String
    targetModel String
    targetId    String
    note        String?
    createdAt   DateTime @default(now())

    @@index([targetModel, targetId])
    @@index([actorId])
  }
  ```
- **ویژگی‌های امنیتی در پنل ادمین**:
  - برای این مدل `permissions: []` تنظیم شده است.
  - هیچ کاربری حتی در سطح ادمین امکان ایجاد دستی (`+ Add`)، ویرایش یا حذف این رکوردها را در پنل ندارد (حالت کاملاً Read-Only).
  - تمام تغییرات بحرانی سیستم (لغو سفارش، تغییر وضعیت سفارش، تایید نمونه‌کار، رد اثر) به صورت اتمیک این رویدادها را ثبت می‌کنند.

---

## ۷. مکانیزم احراز هویت و امنیت ادمین

۱. **متغیرهای محیطی**:
   - `ADMIN_MOBILE`: شماره تلفن همراه مدیر سیستم (مثلاً `09100138383` یا با پیشوند `+98`).
۲. **اعتبارسنجی نشست (Session Guard)**:
   - سیستم با استفاده از توکن JWT رمزنگاری‌شده در کوکی `jar_session` وضعیت کاربر را بررسی می‌کند.
   - تابع `isAdminSession` در `lib/auth/admin.ts` تطابق شماره تماس را با شماره سوپرادمین یا داشتن نقش `role: "admin"` اعتبارسنجی می‌کند.
۳. **حفاظت چندلایه**:
   - در سطح روت صفحه سرور (`app/admin/[[...nextadmin]]/page.tsx`): ریدایرکت خودکار کاربران غیرمجاز به صفحه لاگین با حفظ آدرس بازگشت (`/login?redirect=/admin`).
   - در سطح مسیر API (`app/api/admin/[[...nextadmin]]/route.ts`): بازگرداندن پاسخ ۴۰۱ Unauthorized روی متدهای GET, POST, DELETE در صورت عدم احراز هویت.
   - در سطح توابع Server Action: بررسی مجدد دسترسی پیش از اجرای هرگونه کوئری دیتابیس.

---

## ۸. راهنمای توسعه‌دهنده (Extension Guide)

### چگونه یک مدل جدید اضافه کنیم؟
۱. مدل جدید را در فایل `prisma/schema.prisma` تعریف کرده و دستور `npx prisma migrate dev` یا `deploy` را اجرا کنید.
۲. دستور `npx prisma generate` را اجرا نمایید تا تعاریف جدید کلاینت و تایپ‌های NextAdmin تولید شوند.
۳. در فایل `lib/admin/options.tsx`:
   - نام مدل را به آرایه `models` در یکی از گروه‌های سایدبار اضافه کنید.
   - در شیء `options.model`، ویژگی‌های مدل (عنوان فارسی، آیکون، فیلدهای لیست، فیلدهای ویرایش، جستجو و فرمترها) را تنظیم فرمایید.

### چگونه یک اکشن سفارشی جدید تعریف کنیم؟
۱. اگر اکشن شما نیازمند ورودی کاربر است (مانند دلیل یا توضیحات):
   - یک کامپوننت دیالوگ در `components/admin/` بسازید.
   - سرور اکشن مربوطه را در `app/actions/adminActionHandlers.ts` پیاده کنید (حتماً لاگ حسابرسی `AuditLog` را ثبت کنید).
   - در `options.model[ModelName].actions` یک اکشن از نوع `dialog` با ارجاع به کامپوننت دیالوگ اضافه کنید.
۲. اگر اکشن مستقیم است (مانند تغییر یک فیلد یا تایید):
   - یک Server Action تعریف کرده و آن را در آرایه `actions` با `type: "action"` ثبت کنید.

---
**تیم توسعه و زیرساخت پلتفرم جار**
*آخرین به‌روزرسانی: سپتامبر ۲۰۲۶*
